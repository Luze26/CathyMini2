/**
 * FAQ controller, just for the tick on the navabar
 */
angular.module('faq').
    controller('faqCtrl', ['$rootScope', function($rootScope) {
    
        $rootScope.header = "faq";
        
  }]);



